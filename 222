{
# include <iostream>
    using ramespace std;
    int main() {
        setlocale(0, "");
        cout << "Выберете время года";
        cout1 << "Лето";
        cout2 << "Осень";
        cout3 << "Зима";
        cout4 << "Весна";
        cout << "Введите число от 1-4";
        int num;
        cin >> num;
        if (num = 1) {
            cout << "Сейчас лето,можно одется легко и взять с собой шляпу";
        }
        if (num = 2) {
            cout << "Сейчас осень, следует одеться тепло и взять с собой зонт"
        }
        if (num = 3) {
            cout << "Сейчас зима,нужно одется тепло и взять с собой шарф";
        }
        if (num = 4) {
            cout << "Сейчас весна,можно одется легко и взять с собой зонт";
        }
        retern 0;
    }
